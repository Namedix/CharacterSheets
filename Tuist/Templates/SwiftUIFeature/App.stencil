import SwiftUI
import {{ name }}

@main
struct {{ name }}App: App {
    var body: some Scene {
        WindowGroup {
            {{ name }}View(
				store: .init(initialState: {{ name }}.State(), reducer: {{ name }}())
			)
        }
    }
}
