import SwiftUI
import {{ name }}

@main
struct CthulhuCharacterSheetApp: App {
    var body: some Scene {
        WindowGroup {
            {{ name }}View(
				store: .init(initialState: {{ name }}.State(), reducer: {{ name }}())
			)
        }
    }
}
